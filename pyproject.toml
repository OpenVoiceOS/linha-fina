[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "linha-fina"
dynamic = ["version"]
description = "OVOS pipeline plugin using LinhaFina intent matching"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [{ name = "JarbasAI", email = "jarbasai@mailfence.com" }]
requires-python = ">=3.10"
dependencies = [
    "scikit-learn",
    "joblib",
    "simplematch",
    "rapidfuzz",
    # opm.pipeline entrypoints + the bus-client 2.x stack need modern
    # opm/workshop; bare floors backtrack to ancient releases that still
    # import mycroft_bus_client and break collection.
    "ovos-plugin-manager>=2.3.0a1,<3.0.0",
    "ovos-bus-client>=2.5.1a1,<3.0.0",
    "ovos-config",
    "ovos-utils",
    "langcodes",
    # INTENT-4 SpecMessage registration topics (§6 template) live here
    "ovos-spec-tools>=0.17.3a1",
]

[project.optional-dependencies]
test = ["pytest", "pytest-cov", "ovoscope>=0.17.1a1"]
benchmark = ["padaos", "padatious", "fann2==1.0.7", "nebulento", "datasets"]

[tool.coverage.run]
source = ["linha_fina"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]

[project.urls]
Homepage = "https://github.com/TigreGotico/linha-fina"

[project.entry-points."opm.pipeline"]
"ovos-linha-fina-pipeline-plugin" = "linha_fina.opm:LinhaFinaPipeline"
"ovos-linha-fina-domain-pipeline-plugin" = "linha_fina.opm:DomainLinhaFinaPipeline"
"ovos-linha-fina-hierarchical-pipeline-plugin" = "linha_fina.hierarchical_opm:HierarchicalLinhaFinaPipeline"

[tool.setuptools.dynamic]
version = { attr = "linha_fina.version.__version__" }

[tool.setuptools.packages.find]
where = ["."]
include = ["linha_fina*"]
